# The engines range in package.json is the resolved floor of what the lockfile
# actually needs (eslint 10 and sass 1.103), not a preference. Without this, npm
# only warns and the install "succeeds" - then vite fails much later with an error
# that says nothing about Node. Fail at install time, where the fix is obvious.
engine-strict=true
